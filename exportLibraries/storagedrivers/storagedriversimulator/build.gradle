dependencies {
    compile project(":storagedriver"),
            library(slf4j),
            library(spring),
            library(commons_lang)
}

jar {
    from 'direct_type_metadata.properties'
    from 'provider_type_metadata.properties'
}
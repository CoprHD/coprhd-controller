{
  baseKey:"UnmountBlockVolume.Block.Linux",
  serviceId:"Linux-UnmountBlockVolume",
  fields: {
    host:{
      type:"assetType.vipr.linuxHost"
    },

    volumes: {
      type:"assetType.vipr.mountedBlockResource",   
      select:"many" 
    }            
  }
}

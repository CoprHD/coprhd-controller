{
    baseKey:"SwapContinuousCopies.Block",
    serviceId:"SwapContinuousCopies",
    destructive: false,
    fields: {
        project:{
            type:"assetType.vipr.project",
            lockable: true
        },

        storageType:{
      	    type:"assetType.vipr.blockVolumeOrConsistencyType",
      	    lockable:true,
      	    initialValue:"volume"
        },

        volumes:{
            type:"assetType.vipr.protectedBlockVolume"
        },

        failoverTarget:{
            type: "assetType.vipr.failoverTarget"
        }
    }
}

{
  baseKey:"ObjetStorageService.CreateBucket",
  serviceId:"CreateBucket",
  roles: [
    "HOME_TENANT_ADMIN"
  ],
  fields:{
    name:{
    
    },

    virtualPool:{
      type:"assetType.vipr.objectVirtualPool"
    },
    
    project:{
      type:"assetType.vipr.project",
      lockable: true
    }
  }
}
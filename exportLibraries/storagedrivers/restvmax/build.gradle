dependencies {
    compile project(":storagedriver"),
        library(google_gson),
        library(httpclient4),
        library(log4j),
        library(slf4j),
        library(commons_lang),
        library(jersey)

    testCompile library(easymock)
}


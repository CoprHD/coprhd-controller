dependencies{
    compile \
        library(apache_cassandra), // Required for commons-lang
        library(log4j),
        library(commons_io),
        library(guava),
        "com.toastcoders:yavijava:6.0.01"
}
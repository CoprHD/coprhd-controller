apply plugin: 'scripts'

scripts {
    dbtest {
        mainClass = 'DbTest'
        service = false
        gcDetails = false
    }
}

scripts {
    eventtest {
        mainClass = 'GetEvent'
        service = false
        gcDetails = false
    }
}

dependencies {
    compile library(commons_io),
            library(commons_codec),
            project(":controllersvc"),
            project(":dbsvc"),
            project("vipr-client")

}       

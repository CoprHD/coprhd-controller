{
  baseKey:"CreateCloneOfApplication.Application",
  serviceId:"CreateCloneOfApplication",
  fields:{
  
    application:{
      type:"assetType.vipr.application"
    },
    
    virtualArray:{
      type:"assetType.vipr.applicationVirtualArray"
    },

    virtualPool:{
      type:"assetType.vipr.applicationVirtualPool"
    },
    
    name:{
      type:"text",
      validation:{
        min:2,
        max:128
      }
    },
    
    count: {
      type:"number",
      initialValue:1,
      validation:{
        min:1,
        max:100
      }
    }
  }
}
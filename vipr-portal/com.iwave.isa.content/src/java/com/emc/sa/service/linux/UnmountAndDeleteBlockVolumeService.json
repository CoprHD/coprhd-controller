{
    baseKey:"UnmountAndDeleteBlockVolume.Block.Linux",
    serviceId:"Linux-UnmountAndDeleteBlockVolume",
    destructive: true,
    fields: {
        host:{
            type:"assetType.vipr.linuxHost"
        },

        volumes: {
            type:"assetType.vipr.mountedBlockResourceNoTargets",
            select:"many"
        }
    }
}

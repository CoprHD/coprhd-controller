dependencies {
    compile project(":storagedriver"),
        library(google_gson),
        library(httpclient4),
        library(slf4j),
        library(commons_lang)

    testCompile library(easymock)
}
